# Copy to .env (gitignored) if you need to override this locally. The default already matches
# backend/README.md's local setup (php artisan serve on port 8000).
VITE_API_BASE_URL=http://localhost:8000
